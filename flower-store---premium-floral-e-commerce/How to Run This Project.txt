How to Run This ProjectOpen your terminal and make sure you are in the project’s root folder (the folder where the code files are located). Then run:1. Install dependencies  npm install2. Start the development server      npm run dev3. Open in browserAfter the server starts, open the local URL shown in the terminal (usually something like):4. http://localhost:5173orhttp://localhost:3000